The live website can be found at: https://tristanbarlowgriffin.co.uk/Dissertation/Consent/

The server code may not run correctly locally depending on permissions. If you try and connect with a local server, you may experience CORS errors. so using the website would be better.
But most of the functionality should be avaible with just by running the html files localy.

The passwords have been changed in the python scripts (they're not the actual passwords).

Jest node library along with the pre-commit library are used to run all of the tests in the Test folder. If one of the tests failed the commit will not go through.

 